apiVersion: v1
kind: Service
metadata:
  name: grade-submission-api
  namespace: grade-submission
spec:
  selector:
    app: grade-submission-api
    component: backend
  ports:
  - port: 3000
    targetPort: 3000
    

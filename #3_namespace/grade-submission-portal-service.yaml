apiVersion: v1
kind: Service
metadata:
  name: grade-submission-portal
  namespace: grade-submission
spec:
  type: NodePort
  selector:    
    app: grade-submission-portal
    component: frontend
  ports:
  - port: 5001
    targetPort: 5001
    nodePort: 32000
